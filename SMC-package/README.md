# SMC Package

Общий пакет с бизнес-логикой для генерации учебных документов.

## 📦 Установка

```bash
# Из корня проекта
pip install -e ./SMC-package

# Или через Poetry
cd SMC-package && poetry install
```

## 🧩 Структура

```bash
smc_package/
├── methods/           # Функции для работы с Excel и генерации документов
│   └── Workflow_functions.py
└── schemas/           # Pydantic модели данных
    └── GeneralDict.py