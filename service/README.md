# SMC Service

FastAPI сервис для генерации учебных документов.

## 🚀 Запуск

### Локальный запуск с Poetry

```bash
# Перейти в директорию сервиса
cd service

# Установить зависимости
poetry install

# Запустить сервис
poetry run uvicorn src.main:app --reload --host 0.0.0.0 --port 8000
```

### Локальный запуск с pip

```bash
# Установить локальные пакеты
pip install -e ../SMC-package
pip install -e ../API-lib

# Установить зависимости сервиса
pip install -r requirements.txt

# Запустить
uvicorn src.main:app --reload
```

### Запуск через Docker

```bash
# Из корня проекта SMC
docker build -t smc-service -f service/Dockerfile .
docker run -p 8000:8000 --name smc-container smc-service
```

### 📁 Структура сервиса

```bash
service/
├── src/
│   ├── api/           # HTTP эндпоинты
│   │   └── routes/
│   ├── core/          # Конфигурация
│   ├── services/      # Бизнес-логика
│   └── main.py        # Точка входа
├── templates/         # Word шаблоны
├── uploads/           # Загруженные Excel файлы
├── generated/         # Сгенерированные документы
├── .env.example       # Пример переменных окружения
└── Dockerfile         # Конфигурация Docker