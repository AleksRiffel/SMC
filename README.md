# SMC Project - Система генерации учебных документов

Монорепозиторий для системы генерации рабочих программ, аннотаций и фондов оценочных средств.

## 📋 О проекте

SMC - это система для автоматической генерации учебно-методической документации на основе Excel-файлов с учебными планами.

### Возможности
- Загрузка Excel-файлов с учебными планами
- Извлечение информации о дисциплинах, компетенциях, объемах занятий
- Генерация документов по шаблонам:
  - Рабочая программа дисциплины (РП)
  - Аннотация (А)
  - Фонд оценочных средств (ФОС)
- Скачивание сгенерированных документов

## 🏗 Структура проекта

```bash
SMC/
├── SMC-package/ # Общий пакет с бизнес-логикой
├── API-lib/ # Клиентская библиотека для других сервисов
└── service/ # FastAPI сервис
```

### Компоненты

| Компонент | Назначение |
|-----------|------------|
| **SMC-package** | Ядро системы: функции для работы с Excel, Pydantic модели |
| **API-lib** | Клиентская библиотека для интеграции с другими сервисами |
| **service** | HTTP API на FastAPI, точка входа для пользователей |

## 🚀 Быстрый старт

### Требования
- Python 3.11+
- Poetry (рекомендуется) или pip
- Docker (опционально)

### Локальный запуск

```bash
# Клонировать репозиторий
git clone <url-репозитория>
cd SMC

# Установить зависимости через Poetry
cd service && poetry install

# Запустить сервис
poetry run uvicorn src.main:app --reload
```

### Запуск через Docker

```bash
# Собрать образ
docker build -t smc-service -f service/Dockerfile .

# Запустить контейнер
docker run -p 8000:8000 --name smc-container smc-service
