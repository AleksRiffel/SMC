# SMC API Client Library

Клиентская библиотека для взаимодействия с SMC сервисом.

## 📦 Установка

```bash
# Из корня проекта
pip install -e ./API-lib

# Или через Poetry
cd API-lib && poetry install
```

## 🧩 Структура

smc_api_lib/
├── api/               # Клиент для HTTP API
│   └── client.py      # Основной класс SMCClient
└── schemas/           # Схемы запросов/ответов
    └── api_schemas.py